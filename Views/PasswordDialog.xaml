<Window x:Class="M_A_G_A.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пароль" Width="320" Height="160"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Background="#1A1A1A" Padding="24">
        <StackPanel>
            <TextBlock x:Name="PromptText" Text="Введите пароль:" FontSize="13"
                       Foreground="White" Margin="0,0,0,10"/>
            <PasswordBox x:Name="PwdBox" FontSize="14" Padding="10,8"
                         Background="#2A2A2A" Foreground="White"
                         BorderThickness="0" Margin="0,0,0,14"
                         KeyDown="PwdBox_KeyDown"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Click="CancelClick"
                        Padding="14,7" Margin="0,0,8,0" FontSize="12"
                        Background="#2A2A2A" Foreground="White" BorderThickness="0"
                        Cursor="Hand"/>
                <Button Content="ОК" Click="OkClick"
                        Padding="20,7" FontSize="12" FontWeight="SemiBold"
                        Background="White" Foreground="Black" BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
